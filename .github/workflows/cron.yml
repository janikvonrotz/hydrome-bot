name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Call send notification api
      run: send-notifications.sh
      env:
        TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
